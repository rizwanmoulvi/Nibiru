"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
describe("result", () => {
    const err = "Error";
    test("Result", () => {
        const ResultMock = new _1.Result({ ok: true });
        expect(ResultMock).toBeInstanceOf(_1.Result);
    });
    test("Result - console error", () => {
        const consoleErrorMock = jest
            .spyOn(console, "error")
            .mockImplementation((str) => str);
        new _1.Result({ ok: true, err });
        expect(consoleErrorMock).toHaveBeenCalledWith("ResultError: ok and error states must not be defined simultaneously");
    });
    test("ofSafeExec", () => {
        const result = _1.Result.ofSafeExec(() => {
            throw err;
        });
        expect(result.err).toEqual(new Error(err));
    });
    test("parseError", () => {
        const ResultMock = (0, _1.parseError)(err);
        expect(ResultMock).toEqual(new Error(err));
    });
});
//# sourceMappingURL=result.test.js.map