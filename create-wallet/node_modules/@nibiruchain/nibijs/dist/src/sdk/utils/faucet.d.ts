import { Chain } from ".";
/**
 * Sends 11 NIBI, 100 NUSD, and 100 USDT to the given address from the testnet faucet.
 */
export declare function useFaucet({ address, chain, amts, grecaptcha, }: {
    address: string;
    chain: Chain;
    amts?: {
        nibi: number;
        nusd: number;
        usdt: number;
    };
    grecaptcha: string;
}): Promise<Response | undefined>;
/**
 * Constructs a faucet URL from a Chain object.
 * @param chain a Chain object
 */
export declare const faucetUrlFromChain: (chain: Chain) => string;
//# sourceMappingURL=faucet.d.ts.map